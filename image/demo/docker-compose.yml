services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: imagelite
    ports:
      - '5432:5432'
    volumes:
      - ./data:/var/lib/postgressql/data


  pgadmin:
    image: dpage/pgadmin4
    restart: always
    environment:
      PGADIMIN_DEFAULT_PASSWORD: admin
      PGADIMIN_DEFAULT_EMAIL: admin@admin.com
    ports:
      - '15432:80'
    depends_on:
      - db